"use client";

import Image from "next/image";
import Link from "next/link";
import { Heart, ShoppingBag } from "lucide-react";
import { useCommerce } from "@/components/commerce/CommerceProvider";
function money(pence: number) { return new Intl.NumberFormat("en-GB", { style: "currency", currency: "GBP" }).format(pence / 100); }
export default function WishlistPage() { const { wishlist, addToCart, toggleWishlist } = useCommerce(); return <div className="min-h-screen bg-[#f3eee5] pb-24 pt-40 text-[#1c1814] sm:pt-48"><div className="mx-auto max-w-7xl px-5 sm:px-8"><p className="text-[10px] font-semibold uppercase tracking-[0.28em] text-[#8d6745]">Saved for later</p><h1 className="mt-3 font-heading text-5xl">Wishlist</h1>{wishlist.length ? <div className="mt-10 grid gap-x-8 gap-y-12 sm:grid-cols-2 lg:grid-cols-4">{wishlist.map((item) => <article key={item.slug}><Link className="relative block aspect-3/4 bg-[#e8dfd1]" href={`/products/${item.slug}`}><Image alt={item.name} className="object-contain p-6" fill sizes="(max-width: 640px) 100vw, 25vw" src={item.image} /></Link><div className="mt-4 flex items-start justify-between"><div><Link className="font-heading text-xl" href={`/products/${item.slug}`}>{item.name}</Link><p className="mt-1 text-sm text-black/45">{money(item.pricePence)}</p></div><button aria-label={`Remove ${item.name} from wishlist`} onClick={() => toggleWishlist(item)} type="button"><Heart className="fill-current" size={18} /></button></div><button className="mt-4 flex w-full items-center justify-center gap-2 border border-black px-4 py-3 text-[10px] font-semibold uppercase tracking-[0.17em] hover:bg-black hover:text-white" onClick={() => addToCart(item)} type="button"><ShoppingBag size={14} />Add to cart</button></article>)}</div> : <div className="mt-12 border-y border-black/10 py-20 text-center"><p className="font-heading text-3xl text-black/40">Nothing saved yet</p><Link className="mt-6 inline-flex bg-[#1c1814] px-6 py-3 text-xs font-semibold uppercase tracking-[0.18em] text-white" href="/recreations">Explore fragrances</Link></div>}</div></div>; }
