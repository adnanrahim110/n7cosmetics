"use client";

import Image from "next/image";
import Link from "next/link";
import { ArrowRight, Minus, Plus, Trash2 } from "lucide-react";
import { useCommerce } from "@/components/commerce/CommerceProvider";
function money(pence: number) { return new Intl.NumberFormat("en-GB", { style: "currency", currency: "GBP" }).format(pence / 100); }
export default function CartPage() { const { cart, removeFromCart, updateQuantity } = useCommerce(); const subtotal = cart.reduce((sum, item) => sum + item.pricePence * item.quantity, 0); return <div className="min-h-screen bg-[#f3eee5] pb-24 pt-40 text-[#1c1814] sm:pt-48"><div className="mx-auto max-w-6xl px-5 sm:px-8"><p className="text-[10px] font-semibold uppercase tracking-[0.28em] text-[#8d6745]">Your selection</p><h1 className="mt-3 font-heading text-5xl">Shopping cart</h1>{cart.length ? <div className="mt-10 grid gap-8 lg:grid-cols-[1fr_340px]"><section className="divide-y divide-black/10 border-y border-black/10">{cart.map((item) => <article key={item.slug} className="grid grid-cols-[90px_1fr_auto] gap-4 py-5 sm:grid-cols-[120px_1fr_auto]"><Link className="relative aspect-square bg-white/45" href={`/products/${item.slug}`}><Image alt={item.name} className="object-contain p-2" fill sizes="120px" src={item.image} /></Link><div><Link className="font-heading text-xl hover:text-[#8d6745]" href={`/products/${item.slug}`}>{item.name}</Link><p className="mt-2 text-sm text-black/45">{money(item.pricePence)}</p><div className="mt-4 inline-flex items-center border border-black/15"><button aria-label="Decrease quantity" className="grid size-8 place-items-center" onClick={() => updateQuantity(item.slug, item.quantity - 1)} type="button"><Minus size={13} /></button><span className="w-8 text-center text-sm">{item.quantity}</span><button aria-label="Increase quantity" className="grid size-8 place-items-center" onClick={() => updateQuantity(item.slug, item.quantity + 1)} type="button"><Plus size={13} /></button></div></div><div className="flex flex-col items-end justify-between"><p className="font-medium">{money(item.pricePence * item.quantity)}</p><button aria-label={`Remove ${item.name}`} className="text-black/35 hover:text-red-600" onClick={() => removeFromCart(item.slug)} type="button"><Trash2 size={16} /></button></div></article>)}</section><aside className="h-fit border border-black/10 bg-white/45 p-6"><h2 className="font-heading text-2xl">Summary</h2><div className="mt-5 flex justify-between border-y border-black/10 py-4 text-sm"><span>Subtotal</span><strong>{money(subtotal)}</strong></div><p className="mt-3 text-xs leading-5 text-black/45">Discounts and delivery are calculated securely at checkout.</p><Link className="mt-6 flex items-center justify-between bg-[#1c1814] px-5 py-4 text-xs font-semibold uppercase tracking-[0.17em] text-white" href="/checkout">Checkout <ArrowRight size={16} /></Link></aside></div> : <div className="mt-12 border-y border-black/10 py-20 text-center"><p className="font-heading text-3xl text-black/40">Your cart is empty</p><Link className="mt-6 inline-flex bg-[#1c1814] px-6 py-3 text-xs font-semibold uppercase tracking-[0.18em] text-white" href="/yusuf-bhai-originals">Explore fragrances</Link></div>}</div></div>; }
