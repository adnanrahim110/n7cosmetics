import Link from "next/link";
import { requestPasswordResetAction } from "./actions";

export default async function ForgotPasswordPage({ searchParams }: { searchParams: Promise<{ sent?: string }> }) {
  const { sent } = await searchParams;
  return <main className="grid min-h-screen place-items-center bg-[#f6f5f2] px-5 text-zinc-950"><section className="w-full max-w-sm rounded-2xl border border-zinc-200 bg-white p-7 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[0.22em] text-amber-700">N7 Cosmetics</p><h1 className="mt-3 font-body text-2xl font-semibold tracking-tight">Forgot password</h1><p className="mt-2 text-sm leading-6 text-zinc-500">Enter your administrator email. If the account exists, we’ll email a one-time reset link.</p>{sent ? <div className="mt-5 rounded-lg border border-emerald-200 bg-emerald-50 px-3 py-2 text-sm text-emerald-700">If that account exists, a reset link has been sent. Check your inbox and spam folder.</div> : <form action={requestPasswordResetAction} className="mt-6 space-y-4"><label className="block text-sm font-medium text-zinc-700">Email<input autoComplete="email" autoFocus className="mt-1.5 w-full rounded-lg border border-zinc-300 bg-white px-3 py-2.5 outline-none focus:border-amber-700 focus:ring-2 focus:ring-amber-100" maxLength={190} name="email" required type="email" /></label><button className="w-full rounded-lg bg-zinc-950 px-4 py-2.5 text-sm font-semibold text-white" type="submit">Send reset link</button></form>}<Link className="mt-5 block text-center text-sm font-medium text-amber-800 hover:text-amber-950" href="/admin/login">Back to sign in</Link></section></main>;
}
